# Story BOOK-1: Add "get book by author" endpoint

## Summary
Add a reactive endpoint that returns a single book for an exact (case-insensitive) author-name match. The controller implements the already-generated `BooksApi.getBookByAuthor` method and delegates to `BookService`, which calls `CatalogClient.fetchBookByAuthor(String author)`. All flows must be reactive (Mono) with no blocking.

## API
- HTTP: GET /books/by-author/{author}
- Controller method signature (already declared on generated interface):
  Mono<ResponseEntity<BookResponse>> getBookByAuthor(String author)

## Behaviour and validation
- Exact, case-insensitive match on author name (no partial matching).
- If `author` is null, blank (empty or whitespace-only), or length &gt; 256 characters: return 400 Bad Request.
- The controller delegates to `BookService` (constructor injection).
- `BookService` calls `CatalogClient.fetchBookByAuthor(String author)` which returns `Mono<CatalogBookDto>`.
- Implement `CatalogClient.fetchBookByAuthor` in `CatalogClient` interface and `CatalogClientImpl`.
  - The implementation calls the catalog endpoint: `/catalog/books/by-author/{author}` (HTTP GET).
  - Return type: `Mono<CatalogBookDto>`.
- If the catalog client returns empty: controller returns 404 Not Found.
- If the catalog client returns more than one result (not expected): use the first result returned.

## Reactive constraints
- No blocking: do not call `.block()` or `.collectList()` anywhere.
- All composition must use Reactor (`Mono` chaining / map / flatMap / switchIfEmpty etc.).

## Files to modify (coding phase only)
- Controller (implements generated interface; do NOT edit generated OpenAPI module):
  - src/main/java/.../controller/<BooksController or existing controller>.java
    - Implement `getBookByAuthor(String author)` as declared on `BooksApi`.
    - Validate `author` (blank / length) and return 400 as appropriate.
    - Delegate to `BookService` and map result to `ResponseEntity.ok(body)`.
    - On empty result produce `ResponseEntity.notFound().build()`.

- Service:
  - src/main/java/.../service/BookService.java (or existing service)
    - Add method: `Mono<BookResponse> findByAuthor(String author)` (or adapt existing naming).
    - Delegate to `CatalogClient.fetchBookByAuthor(author)` and map `CatalogBookDto` → `BookResponse`.

- Catalog client:
  - src/main/java/.../client/CatalogClient.java
    - Add method signature: `Mono<CatalogBookDto> fetchBookByAuthor(String author)`.
  - src/main/java/.../client/impl/CatalogClientImpl.java
    - Implement HTTP GET to `/catalog/books/by-author/{author}` using WebClient (reactive).

- DTO mapping:
  - Reuse existing `BookResponse` and `CatalogBookDto` mapping approach used by `getBookById`.
  - No new DTOs unless necessary; prefer existing converters/mappers.

## Error handling
- Validation errors → HTTP 400.
- No match → HTTP 404.
- Propagate other errors as per existing global error handling (do not add new resilience logic).

## Unit testing (unit_testing phase)
- Add tests for controller/service using JUnit 5 + Reactor StepVerifier.
- Mock `CatalogClient` to return a Mono of `CatalogBookDto` for the happy path and Mono.empty() for not-found.
- Assert the `Mono<ResponseEntity<BookResponse>>` returns 200 and correct body for happy path.
- Assert 404 for empty result, and 400 for blank or >256 author values.
- Use StepVerifier to validate reactive sequences. No external calls.
- Aim for ≥ 90% line coverage on the changed class (this is enforced by pipeline).

## Notes / Decisions (explicit)
- Path variable arrives URL-decoded by framework — accept as-is.
- No authentication required for this sample endpoint.
- No timeouts/retries/circuit-breakers/rate-limiting are added here.

## Example controller snippet (conceptual)
```
@Override
public Mono<ResponseEntity<BookResponse>> getBookByAuthor(String author) {
  if (author == null || author.trim().isEmpty() || author.length() > 256) {
    return Mono.just(ResponseEntity.badRequest().build());
  }
  return bookService.findByAuthor(author)
    .map(book -> ResponseEntity.ok(book))
    .map(ResponseEntity::ok)
    .map(Function.identity())
    .switchIfEmpty(Mono.just(ResponseEntity.notFound().build()));
}
```
(Use correct mapping and avoid redundant map calls; above is illustrative.)

## Acceptance Criteria (recap)
- AC1: Controller provides Mono<ResponseEntity<BookResponse>> for GET /books/by-author/{author}.
- AC2: Controller delegates to BookService; no blocking.
- AC3: Lookup uses CatalogClient.fetchBookByAuthor(...).
- AC4: Reactive patterns followed.
- AC5: Empty catalog result → 404; blank or >256 → 400.
- AC6: Unit tests using StepVerifier cover happy path and edge cases; changed class has ≥ 90% line coverage.
- AC7: Controller implements generated `BooksApi.getBookByAuthor` (do not edit OpenAPI-generated module).


---
Generated by developer request for Story BOOK-1.
